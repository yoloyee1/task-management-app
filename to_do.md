# 專案名稱：智慧零售 2D 數位孿生模擬系統

## 專案背景與目標

請扮演資深的 Godot 開發者。我需要利用 Godot 引擎開發一個 2D 的數位孿生雛形，用於模擬實體零售店鋪的庫存消耗與補貨機制。
系統必須讀取 Kaggle 的零售歷史數據集 (`train.csv`)，透過計時器模擬時間推移，並在 2D 平面圖上即時反映各貨架的庫存狀態。當庫存過低時，系統需自動在 UI 介面上生成補貨任務。本專案不涉及後端資料庫，所有任務皆在前端 UI 呈現。

## 開發任務拆解

### 1\. 場景與 UI 建置 (Scene \& UI Setup)

* **2D 店面視圖：** 建立一個 2D 場景 (MainScene)。使用簡單的色塊或 `Sprite2D` 代表不同的「貨架 (Shelf)」。
* **貨架節點 (Shelf Node)：** 每個貨架需要顯示對應的 `product\_id`，以及當前的「剩餘庫存量 (Stock)」。
* **可選任務列表 (Task List UI)：** 在畫面的邊緣（如右側）建立一個 `Control` 節點組成的面板（可使用 `VBoxContainer` 或 `ItemList`）。此面板用於動態顯示系統自動生成的「補貨任務」。請加入一個按鈕，讓使用者可以自由展開/收起這個任務列表。

### 2\. 數據讀取與模擬 (Data Simulation)

* **讀取 CSV：** 撰寫腳本使用 Godot 的 `FileAccess` 讀取專案目錄下的 `train.csv`。
* **資料過濾：** 為了畫面整潔，請只篩選特定一家店鋪（例如 `store\_id == "1"`）的資料。
* **時間推移：** 實作一個 `Timer` 節點（例如每 1 秒觸發一次）。每次 Timeout 時，讀取 CSV 的下一筆同日期的銷售紀錄，並根據 `number\_sold` 扣除對應貨架的庫存。

### 3\. 狀態管理與視覺反饋 (State Management \& Visuals)

* 為每個商品設定一個初始庫存量（例如 100）與安全水位（例如 20）。
* **程式碼規範限制（重要）：** 為了確保未來專案擴充時的程式碼可維護性，避免使用臃腫的 `if-elif` 結構。在判斷庫存水位與處理狀態分發時，**必須強制使用 `match` 語句來撰寫邏輯**。

**邏輯參考範例：**

```gdscript
match current\_stock\_state:
    "NORMAL":
        # 庫存充足，貨架 Sprite 顯示原本顏色（如白色）
    "WARNING":
        # 低於安全水位，貨架 Sprite 閃爍黃色
    "EMPTY":
        # 缺貨，貨架 Sprite 變為紅色，並觸發 UI 任務生成

